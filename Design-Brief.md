# 设计规范：SeedVR2 单页桌面界面

关联：`Product-Spec.md` 的 FLOW-001、REQ-001～REQ-003。

## 1. 设计方向

| 维度 | 方向 |
|---|---|
| 产品形态 | Windows 单页界面型桌面工具 |
| 单一目标 | 让用户快速配置并完成一次单图 4× 放大 |
| 气质 | 利落、可信、现代、克制、有一点大胆 |
| 信息密度 | 中等；主流程一屏完成，日志保持辅助层级 |
| 第一印象 | 专业但不沉闷 |

设计原则：

- DP-001：主任务优先于装饰；参考图中的发票导航、图表和头像不进入产品。
- DP-002：明亮荧光色只用于主操作和进度，避免所有元素争抢注意力。
- DP-003：状态必须同时依靠文字、位置和控件状态表达，不只靠颜色。

正向参考：用户提供的 Salesforce Invoices 概念稿，保留其浅色底、深色大容器、冰蓝嵌套面板、荧光黄重点、圆角与留白；同类工具参考 Topaz Photo 的“主工作区优先”和 Upscayl 的单任务直达。反参考：传统灰色 LabelFrame 堆叠、过多导航、模拟数据仪表盘、装饰性头像与无功能图标。

## 2. 信息架构

单页、无导航：

| 页面 | 目的 | 关联 | 优先级 |
|---|---|---|---|
| SCREEN-001 主任务页 | 配置、运行并查看一次放大任务 | FLOW-001 / REQ-001～003 | P0 |

页面自上而下为：品牌与运行方式标签 → 大标题 → 固定能力摘要 → 深色主任务区。深色主任务区左侧为输入与操作，右侧冰蓝面板为状态、进度和日志。

## 3. SCREEN-001 页面规格

内容层级：

1. “开始 4× 放大”主按钮与当前 Worker 状态。
2. 输入图片、输出目录和任务进度。
3. 固定参数摘要、风险警告和详细日志。

状态：

- 默认/启动：状态面板显示正在启动或检查 CUDA，主按钮禁用。
- 空状态：输入路径为空，提示选择图片；不伪装成错误。
- 运行：路径锁定，荧光进度条推进，停止按钮按阶段启停。
- 错误：状态标题和对话框给出错误，日志保留线索，恢复可编辑路径。
- 成功：显示尺寸、耗时、文件大小和显存，启用打开目录。
- 取消：明确显示任务已停止，恢复下一次任务入口。

桌面响应：默认约 920×680；最小宽度下保持两栏但压缩留白，关键按钮始终可见。该产品不规划移动端。

## 4. 组件

| 编号 | 组件 | 方向 |
|---|---|---|
| CMP-001 | 摘要卡 | 冰灰蓝底、四项固定能力、数字优先 |
| CMP-002 | 路径输入 | 浅色输入面、深色文字、紧邻“浏览”按钮 |
| CMP-003 | 主按钮 | 荧光黄底、深色粗体；禁用态明显降饱和 |
| CMP-004 | 次按钮 | 浅色或描边处理，不与主按钮竞争 |
| CMP-005 | 状态面板 | 冰蓝背景，状态标题优先，深色日志嵌套 |
| CMP-006 | 风险提示 | 暖黄色标签 + 完整警告文案，始终可见 |

按钮、输入框必须保留 Qt 原生键盘与焦点行为；悬停/激活使用轻微明暗变化，仅保留已确认的短交互动效。

## 5. 视觉系统

### 色彩

| Token | 值 | 用途 |
|---|---|---|
| background | `#EEF3F1` | 页面背景 |
| surface | `#F8FAF9` | 浅色卡片与输入 |
| surface-soft | `#DCE7EC` | 摘要卡 |
| surface-dark | `#242624` | 主任务容器、日志 |
| surface-blue | `#AFC7D4` | 状态面板 |
| accent | `#DFFF00` | 主操作、进度与重点 |
| text-primary | `#151815` | 浅色面主文本 |
| text-on-dark | `#F5F7F3` | 深色面主文本 |
| text-secondary | `#667069` | 辅助文本 |
| border | `#D5DEDA` | 克制分割线 |
| warning | `#9A5B00` | 风险提示 |

荧光黄只配深色文字，不使用白字；正文与背景保持清晰对比。

### 排版与形状

- 字体：Microsoft YaHei UI；标题 24～28 px 粗体，区块标题 12～14 px 粗体，正文 9～11 px。
- 间距：以 8 px 为基准，常用 8/12/16/24。
- 大容器 24 px 视觉圆角，嵌套面板 18～20 px；输入和按钮保持 8～12 px 友好感。
- 不使用渐变、玻璃拟态、发光阴影或装饰性动效。

## 6. UX 文案与反馈

- 文案清晰、直接、专业；按钮以动作开头。
- 空状态必须告诉用户下一步；错误必须说明能否重试。
- 进度条上方始终有文字状态，日志不承担唯一反馈职责。
- AI 重绘风险警告不可折叠或隐藏。

## 7. 实现约束与假设

- 使用 PySide6/Qt Widgets 原生布局、Style Sheet 与 `QPainter`；不再用 Pillow 位图模拟普通控件背景。
- 不修改 Worker 协议、推理逻辑、固定参数或输出规则。
- 窗口拖动缩放时内容必须实时跟随，不得在 `resizeEvent` 中解码图片或重建整块背景位图。
- 本次用 Qt 原生绘制近似参考图气质，不追求网页 CSS 级逐像素复刻。
- 待视觉验收：若用户希望加入图片预览，应另立功能范围，避免把本次纯 UI 改版扩大成图像工作台。
